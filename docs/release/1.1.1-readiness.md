# Local Find 1.1.1 (RC2) 发布候选就绪报告

**日期**: 2026-09-24
**状态**: 本地构建完成、已自测；**未上传 Google Play，未提交 Chrome Web Store**

---

## 1. 版本身份

| 项目 | 值 |
|---|---|
| applicationId | `io.github.linkwutcreate.localfind` |
| versionCode | `4`（此前 `3` 已于 2026-09-13 上传 Play 送审，Play 不接受同一 versionCode 的不同二进制，本轮升级） |
| versionName | `1.1.1` |
| minSdk | `26` |
| targetSdk | `36` |
| compileSdk | `36` |
| AGP | `8.9.1` |
| Gradle | `8.11.1` |
| Kotlin | `1.9.22`（编译器）/ 解析出 `kotlin-stdlib 2.0.21` |

## 2. 本轮改动

- **厂商后台设置首次引导**：首次点击"启动服务"时自动弹出分步引导（此后可通过卡片内按钮随时重新打开），
  按手机厂商（小米/华为荣耀/OPPO/vivo/魅族有具体跳转 Intent；三星/原生 Android 无稳定公开跳转，
  给出文字引导 + 落到应用详情页）给出分步说明，末尾提供"我已设置，测试一下"自检按钮（读服务运行状态
  + 电池优化豁免状态，只读检查，不申请新权限）。详见 `android/app/src/main/java/io/github/linkwutcreate/
  localfind/onboarding/`。
- **`tools/build-android.ps1`**：把上一轮临时验证的 ASCII 目录联接方案固化为脚本；本轮修了脚本自身的
  一个 PowerShell 5.1 坑（`$ErrorActionPreference=Stop` 会把 gradlew 任何 stderr 警告升级成终止错误）。
- 版本号 `versionCode 3→4`、`versionName 1.1.0→1.1.1`，Chrome 扩展 `manifest.json` 同步对齐到 `1.1.1`。

## 3. 产物路径与 SHA256

| 产物 | 路径 | 大小 | SHA256 |
|---|---|---:|---|
| Release AAB | `android/app/build/outputs/bundle/release/app-release.aab` | 20,495,055 B | `174F97179BE87234D8735807D7A743251C84D7B65CD862EB4F5732234D5B2233` |
| Release APK（未用于 Play，供本地/侧载测试） | `android/app/build/outputs/apk/release/app-release.apk` | 32,808,102 B | `01446513B16CB286F3361BBC5A8C891C8F82384DD7F2AF0229E2315A3A5BB6F7` |
| Chrome 扩展 RC2 包 | `local-find-release/local-find-chrome-extension-1.1.1.zip` | 31,680 B | `F5903A5D68BAF646D2BFDF48A355FA77B8383D0EB09F25BE71EA8E25C779C52B` |

AAB/APK 均未纳入 git；`local-find-release/` 在 `.gitignore` 中。扩展包本轮改用 Python `zipfile` 重新打包
（条目路径统一用正斜杠），修正了 1.1.0-readiness.md 记录的「PowerShell `Compress-Archive` 生成反斜杠路径」
已知问题。

## 4. 签名验证

| 检查 | 结果 |
|---|---|
| AAB 是否签名 | ✅ `keytool -printcert -jarfile` 读出 `CN=Local Find Upload` |
| 证书 SHA-256 指纹 | `32:59:7A:5D:D1:FD:AA:B3:D5:7D:6D:FD:9F:E9:03:93:FF:D4:FE:29:E2:8F:09:C2:F9:7C:9C:A0:CB:C0:A8:53` |
| 与已在 Play Console 注册的上传证书一致 | ✅ 指纹与 1.1.0-readiness.md / `PLAY_UPLOAD_RUNBOOK.md` 记录的完全相同——同一枚上传密钥 |
| APK 用 `keytool -jarfile` 校验 | 报「不是已签名的 jar 文件」——**预期行为**：现代 AGP 默认用 APK 签名方案 v2/v3，签名块不在传统 JAR/META-INF 里，`keytool` 的 `-jarfile` 校验器认不出；上一轮（1.1.0）证据同样只验证了 AAB，未验证过 APK，本轮沿用同一验证边界，如实记录而非回避 |
| 16 KB 内存页对齐 | ✅ `zipalign -c -P 16 -v 4` → `Verification successful` |

签名密钥本身（`.jks` 文件、密码）本会话全程未读取、未打印、未移动；仅通过 Gradle 签名任务间接使用。

## 5. 自动化测试与构建结果

| 检查项 | 状态 | 证据 |
|---|---|---|
| `./gradlew testDebugUnitTest`（Gradle 方式，通过 `tools/build-android.ps1` 走 ASCII 联接） | ❌ 仍 BLOCKED | 见 §5.1 |
| 测试逻辑本身（绕开 Gradle worker，直接 `java -cp` 验证） | ✅ PASS | `DiscoveryEndpointPolicyTest`(9) + `VendorGuideTest`(16) 一起跑 → `OK (25 tests)` |
| `./gradlew lintRelease` | ✅ PASS | 0 error，62 warning（较 1.1.0 版本的 58 条增加，新增部分来自 onboarding 新文件，均为既有建议类，非 error） |
| `./gradlew assembleRelease` | ✅ PASS | `app-release.apk` 已生成 |
| `./gradlew bundleRelease` | ✅ PASS | `app-release.aab` 已生成并签名 |

`lintRelease assembleRelease bundleRelease` 一次组合执行耗时 **53 秒**，63 个任务（30 执行 / 33 已是最新）。

### 5.1 `testDebugUnitTest`（Gradle 方式）——ASCII 联接方案验证结果：无效，有新证据

本轮按任务包指示建立了 `D:\AIProjects\cache\lf-ascii` 目录联接（`New-Item -ItemType Junction`，
等价 `mklink /J`）指向本仓，并通过联接路径运行 `testDebugUnitTest`。**结果：仍然失败**，但失败点比
1.1.0 轮更深一层：

- 1.1.0 轮：Gradle 测试 worker 进程**自身启动失败**（`ClassNotFoundException: GradleWorkerMain`）。
- 本轮（通过联接）：worker **成功启动**，只是加载不到具体的测试类（`ClassNotFoundException:
  io.github.linkwutcreate.localfind.server.DiscoveryEndpointPolicyTest` /
  `...onboarding.VendorGuideTest`）。

**根因实锤**：检查 Gradle 写出的 worker classpath 临时文件（`D:\AIProjects\cache\lf-gradle\.tmp\
gradle-worker-classpath*.txt`）发现，其中的路径**被解析回了原始中文路径**
（`D:\AIProjects\projects\local-find（找手机app）\android\...`），而不是联接路径
（`D:\AIProjects\cache\lf-ascii\android\...`）。这是 Windows/Java 的既定行为：NTFS 目录联接是文件系统
重解析点，Java 对项目目录做 `getCanonicalPath()` 时会把联接**规范化解析回真实目标路径**——这正是
联接机制的设计本意，否则联接就不透明了。因此"建联接绕开中文路径"这个思路，在 Gradle/Java 的路径规范化
面前天然失效，**不是配置没调对，是这个技术方案本身对这一层问题无效**。

翻查 2026-09-14 的构建日志（`android/build-debug-subst-20260914.log`）发现，**`subst` 盘符映射方案
当时实测能跑通到 `assembleDebug`**（只在写一个无关的诊断报告文件时报权限错误，非编码问题）——
`subst` 是驱动器号级别的 DOS 设备映射，不会被同样的路径规范化机制穿透。本会话原地复验 `subst` 时被
Claude Code 的 auto 模式安全分类器以「Unauthorized Persistence」拦截（任务包只白名单了 `mklink /J`
联接，未提及 `subst`），已写入 `_dispatch/questions/LOCALFIND-RC2-0924.md` 请求用户裁决是否授权，
截至本文档落盘时**未获裁决，未执行**。

**代码正确性的独立证明**（绕开 Gradle 测试 worker）：直接用同一份编译产物 + 依赖 jar 通过原生
`java -cp ... org.junit.runner.JUnitCore <TestClass>...` 运行两个测试类，**25 个测试全部通过**，
证明失败完全在 Gradle 自身机制，与被测代码或测试代码的正确性无关。

**真正能修的路径**（超出本次授权范围，未执行）：`subst` 盘符映射（待用户裁决）、Windows 区域设置里
开启 UTF-8 全球语言支持（任务包明确禁止）、或把仓库整体迁移到纯 ASCII 路径（超出本次任务范围）。

## 6. 真机 / 设备测试

| 检查 | 结果 |
|---|---|
| 手机共享锁状态 | 本文档落盘时仍被 `PRIVCOMM-DEVICE-E2E-0924` 持有，按协议排队等待，未开始 |
| 覆盖安装 RC2 APK、引导页、`/ping`、锁屏控制、停止不自启 | **本文档落盘时 NOT RUN**——见状态文件与后续证据文档的追加记录 |

## 7. 上传步骤（供用户参考；本会话不执行）

同 `1.1.0-readiness.md` 第 6 节，版本号改为 `versionCode 4` / `versionName 1.1.1`。

## 8. 边界声明

本文档及本轮构建：

- 未执行 `git push`、未合并分支、未打 tag；
- 未上传 Google Play、未提交 Chrome Web Store、未访问 Play Console 或 CWS 开发者后台；
- 未读取、打印、移动或复制签名密钥文件本身或其密码；
- 未修改 `applicationId`、配对协议、Chrome 扩展通信协议；
- 未申请新的敏感权限（厂商引导复用清单中已有的 `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`，
  仍走系统弹窗，不做静默豁免）；
- `subst` 方案未经用户裁决，未执行；未改任何系统区域设置。
