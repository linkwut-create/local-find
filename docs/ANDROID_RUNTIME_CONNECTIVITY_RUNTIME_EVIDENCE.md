# Android Runtime Connectivity Evidence

Evidence date: 2026-05-25

Scope: A-DIAG.1 read-only adb runtime evidence collection for the affected phone. This pass does not modify Android code, Chrome extension code, manifests, Gradle files, `MainActivity`, `HttpServerManager`, or `FindPhoneForegroundService`. It does not build, install, uninstall, clear app data, upload, move tags, modify releases, restore stash, reset the repository, or run destructive adb commands.

## Repository Baseline

Command:

```text
git status --short
```

Result:

```text

```

Command:

```text
git describe --tags --dirty
```

Result:

```text
mvp-u5-ok-24-g6b1ff49
```

Command:

```text
git log --oneline -5
```

Result:

```text
6b1ff49 docs: diagnose Android runtime connectivity regression
2c8bb80 tools: add Chrome Web Store screenshot profile helper
848724d docs: add manual Chrome Web Store screenshot capture instructions
ae423ef docs: choose Chrome Web Store screenshot capture strategy
1ab80e4 docs: prepare safe Chrome Web Store screenshot preflight
```

## adb Location

Preferred adb path exists:

```text
C:\Users\Zero\AppData\Local\Android\Sdk\platform-tools\adb.exe
```

For the commands below:

```powershell
$ADB = "C:\Users\Zero\AppData\Local\Android\Sdk\platform-tools\adb.exe"
```

## Device Attachment

Command:

```powershell
& $ADB devices
```

Result:

```text
ADB=C:\Users\Zero\AppData\Local\Android\Sdk\platform-tools\adb.exe
List of devices attached
461QYFFT225UP	unauthorized

* daemon not running; starting now at tcp:5037
* daemon started successfully
```

Interpretation:

- One physical device was visible to adb, serial `461QYFFT225UP`.
- The device state was `unauthorized`, not `device`.
- There was no authorized adb device available for shell, dumpsys, netstat, ip, or logcat collection.
- Per A-DIAG.1 rules, shell/logcat/package-state steps were not run because the affected phone was not authorized.

Required user action before rerunning A-DIAG.1:

1. Reconnect the affected phone by USB.
2. Ensure Developer options and USB debugging are enabled.
3. Accept the "Allow USB debugging?" prompt on the phone.
4. If the prompt does not appear, revoke USB debugging authorizations on the phone, reconnect, and authorize this computer.
5. Confirm `& $ADB devices` reports the affected phone as `device`.

## Package State

Not collected in this pass because adb reported only an unauthorized device.

Planned read-only commands after authorization:

```powershell
& $ADB shell pm list packages | findstr local
& $ADB shell dumpsys package io.github.linkwutcreate.localfind
& $ADB shell dumpsys package com.example.localfind
```

Evidence still needed:

- Whether both `com.example.localfind` and `io.github.linkwutcreate.localfind` are installed.
- `versionCode` / `versionName`.
- `firstInstallTime` / `lastUpdateTime`.
- Launcher activity.
- Requested and granted permissions.
- Runtime `targetSdk`.

## Manual Service Start Instruction

Not performed by adb. After device authorization, the user should manually:

1. Open Local Find on the phone.
2. Enter Find Me / controlled-device mode.
3. Start the service.
4. Keep the phone awake and keep Local Find in the foreground while read-only checks run.

## IP and Listener Evidence

Not collected in this pass because adb was unauthorized.

Planned read-only commands after authorization and manual service start:

```powershell
& $ADB shell "ip addr"
& $ADB shell "toybox netstat -an | grep 8888"
& $ADB shell "ss -ltn | grep 8888"
& $ADB shell "dumpsys connectivity"
```

Evidence still needed:

- Wi-Fi / `wlan` IPv4 address.
- Whether the app-displayed IP matches the Wi-Fi IPv4 address.
- Whether port `8888` is listening.
- Listener address: `0.0.0.0:8888`, `127.0.0.1:8888`, `:::8888`, or no listener.

## Logcat Capture

Not collected in this pass because adb was unauthorized.

No `D:\local-find-android-connectivity-logcat.txt` evidence file was generated by this pass.

Planned steps after authorization:

```powershell
& $ADB logcat -c
```

Then manually reproduce:

1. On the phone itself, open:

```text
http://<phone-ip>:8888/device-info
```

2. From the computer or tablet, open the same URL:

```text
http://<phone-ip>:8888/device-info
```

3. Wait until the external access times out or fails.

Then capture:

```powershell
& $ADB logcat -d > D:\local-find-android-connectivity-logcat.txt
```

Relevant search terms for extraction:

```text
LocalFind
HttpServerManager
MainActivity
FindPhoneForegroundService
ForegroundService
Pairing
token
auth
8888
exception
bind
server
device-info
Ktor
Netty
watchdog
IP
```

## Diagnosis Table

| Check | Result | Interpretation |
| --- | --- | --- |
| adb device attached? | One serial visible: `461QYFFT225UP`, state `unauthorized`. | A phone is connected but this computer is not authorized for adb access. |
| Authorized adb device available? | No. | Runtime shell/logcat evidence cannot be collected yet. |
| package(s) installed? | Not collected. | Requires authorized adb shell. |
| old package exists? | Not collected. | Need `dumpsys package com.example.localfind` after authorization. |
| new package exists? | Not collected. | Need `dumpsys package io.github.linkwutcreate.localfind` after authorization. |
| Wi-Fi IP from `ip addr` | Not collected. | Need authorized adb shell and manual service start. |
| 8888 listener result | Not collected. | Need `netstat` / `ss` after service is manually started. |
| listener address | Not collected. | Cannot classify localhost-only vs broad-bind in this pass. |
| log shows server started? | Not collected. | Need logcat after authorization. |
| log shows request from phone self-access? | Not collected. | Need logcat capture during reproduction. |
| log shows request from external access? | Not collected. | Need logcat capture during external timeout. |
| external timeout produced any server log? | Not collected. | This is still the key branch discriminator. |
| likely next branch | Authorize adb, then rerun A-DIAG.1 runtime evidence collection. | No code fix branch should start from this pass alone. |

## Interpretation Rules For Next Authorized Run

If listener is `127.0.0.1:8888`:

- Conclusion: runtime localhost-only.
- Next step: inspect `HttpServerManager` / Ktor startup path.

If listener is `0.0.0.0:8888` or `:::8888`, but external devices still time out:

- Conclusion: server broad-bind is working.
- Next focus: phone Wi-Fi IP, phone system inbound restrictions, VPN/network isolation, and whether the app displays the wrong IP.

If there is no `8888` listener:

- Conclusion: the service did not truly start, or started and exited.
- Next focus: logcat server start errors, watchdog behavior, foreground service exceptions, and package/runtime state.

If `ip addr` Wi-Fi IP and the app-displayed IP differ:

- Conclusion: `NetworkUtil` may be choosing the wrong interface.
- Next step: plan a minimal fix that prefers the WLAN interface.

If external access produces no server request log:

- Conclusion: the request is not reaching the app.
- Next focus: network path, listener address, IP selection, phone OS inbound restrictions, or LAN isolation.

If external access produces request logs but returns failure:

- Conclusion: the request reaches the app and fails inside routing/auth/handler logic.
- Next focus: token/auth, route handling, or endpoint behavior.

## Immediate Next Recommendation

The current pass is blocked at adb authorization, not at app runtime. The next action is to authorize USB debugging on the affected phone and rerun A-DIAG.1.

After authorization:

- If evidence points to listener/IP/`NetworkUtil`, enter A-FIX.0 with a minimal fix plan.
- If evidence points to phone system inbound restrictions, record a phone settings checklist before changing app code.
- If old and new package IDs coexist in a suspicious state, enter a package migration cleanup test plan.
- If evidence remains insufficient, perform the old working tablet APK on same phone A/B test only after explicit user approval.
